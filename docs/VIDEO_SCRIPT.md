# 7-MINUTE VIDEO SCRIPT — `regime-narrative`

**Total runtime 7:00.** Narration word counts are pitched at ~145 wpm with pauses. All figures below are the measured values in `outputs/_summary_for_agents.json`.

| Time | On screen | Narration |
|---|---|---|
| **0:00–0:25**<br>(25s) | Cold open. The regime chart from the Overview view, full width — SPY on a log axis, calm bands in pale blue, stressed bands in pale red, twenty vertical transition lines. No text overlay yet. At 0:18 a single word fades in over the chart: **"Why?"** | Every regime model produces a picture like this. Calm here. Stressed here. It is a real statistical result, and it is genuinely useful. But the model only ever saw returns. It has never read a newspaper. So it can tell you the market changed on this day — and it cannot tell you anything about why. In practice a human fills that gap from memory. That interpretation is undocumented, unauditable, and different for every person who looks at the chart. This project attaches an explanation to each of those lines, and then measures how much the explanation is actually worth. |
| **0:25–1:00**<br>(35s) | Overview view. Cursor moves along the five metric tiles as each is named: Out-of-sample vol ratio **2.18×**, Forward-20d **1.69×**, Per-fold median **1.52×**, In-sample 2.32×, Transitions 20. Tooltip for the 2.18× tile is opened so the CI **[1.76, 2.69]** is visible. | Start with the part that has nothing to do with language models. A two-state Gaussian HMM on SPY, refitted inside each of twenty-seven walk-forward folds. Scored only on held-out days. Out-of-sample, the stressed state has two-point-one-eight times the volatility of the calm state. Ninety-five percent interval, one-point-seven-six to two-point-six-nine, block bootstrapped, because daily returns are serially dependent. Three thousand four hundred out-of-sample days. The prior notebook I built this on reported two-point-three-three, fitted once over the whole history. That number survives being done properly. It just gets smaller. |
| **1:00–1:35**<br>(35s) | Split screen. Left: `hmm_model.py`, the forward-filter function highlighted, with the line where `predict()` / Viterbi is deliberately **not** used. Right: the "Markets it was never fitted to" table — Nikkei 1.54×, Hang Seng 1.46×, DAX 1.87×. Then the dwell-sensitivity chart with the red pre-registered 20-day line. | Two details that decide whether this is real. First, states are assigned by a forward filter — the probability of the state given observations up to that day only. The standard `predict` call runs Viterbi over the whole sequence, so a day's label can depend on days that came after it. That would contaminate every transition date, and everything later in this project rests on those dates being knowable at the time. Second, generalisation. Same procedure, refitted without modification on three indices outside the fitted universe. One-point-five-four, one-point-four-six, one-point-eight-seven. Weaker than the home market. I would rather say that than show three tidy successes. And eighty-four raw state flips reduce to twenty-four under a pre-registered twenty-day persistence filter, of which twenty recur across random seeds. |
| **1:35–2:00**<br>(25s) | Slow zoom into one transition line on the chart — 29 June 2015. Everything else desaturates. The tooltip shows only: `2015-06-29 · calm → stressed · seed support 90%`. Deliberately no news, no label. | So here is a transition. Twenty-ninth of June, twenty-fifteen. High confidence, nine out of ten seeds. And this is the ceiling of what a returns-only model can ever say. Calm to stressed. That is the entire output. Everything a human wants to know — what happened, whether it was foreseeable, whether it will happen again — is outside the model's sensory range. That is the gap. |
| **2:00–2:40**<br>(40s) | Controls view → Hindsight tab. Metric row: 60 windows, 10,888 items, **0 retrieval failures, 0 post-boundary survivors**. Then the leak table: mean **38.2%**, max **71.2%**, with the 26 Jan 2018 row highlighted — 1,266 bytes pinned, 4,401 bytes today, last edited 2025. Cut to `news/base.py`, the `NewsWindow` constructor raising on a post-boundary item, then `pytest` output showing the boundary tests pass. | The fix is to give a language model the news, and only the news that existed at the time. Wikipedia's Current Events Portal, one page per day, fourteen days per window, sixty windows, nearly eleven thousand items. Two things make that defensible rather than decorative. The boundary is structural. A window object refuses to construct if a single item post-dates it. That is enforced by the type, not by a comment, and twenty-six tests hold it down. And every page is pinned to its revision id — the last edit at or before the boundary. This matters more than it sounds. Wikipedia keeps editing the page for a given day for years. Across these windows, thirty-eight percent of the current page was written after the boundary on average. Worst case, seventy-one percent. The page for the twenty-sixth of January twenty-eighteen was twelve hundred bytes on the day and was last edited in twenty-twenty-five. Fetch the live page and you feed the model hindsight, invisibly, while your manifest says the window closed on the transition date. |
| **2:40–3:20**<br>(40s) | **REQUIREMENT (b).** Provenance view → Prompt tab. `prompts/narrative_v1.md` on screen with its sha256 and the caption "used unchanged for transitions and controls alike". Three highlights land in sequence: (1) the frontmatter `notes:` block; (2) Rule 2 — *every factual claim must cite at least one item id*; (3) Rule 4 — *you may decline*, with the sentence "a confident story about an ordinary fortnight is worse than no story" boxed. Then a two-second cut to `docs/PROMPT_HISTORY.md`. | This is the system prompt. One file, version controlled, hashed, never an inline string. Three lines in it are load-bearing, and each one exists because of a specific failure mode. First, the frontmatter. The same prompt is used for real transitions and for control dates. An earlier draft framed controls neutrally and transitions assertively. That would have measured my framing instead of the news, and the control result would have been an artefact. This is written into the file so nobody later improves one arm. Second, every factual claim must cite an item id. That is not presentation. It is what makes memorisation measurable — a claim whose cited item does not contain it is recall leaking in, and I can count that. Third, the model is explicitly told it may decline, and that declining is a correct answer. Without that line the model confabulates on control dates and the whole comparison collapses to "the model always says something". Every one of those decisions is dated in a prompt history file, written as it happened, not reconstructed afterwards. |
| **3:20–3:50**<br>(30s) | **Live view, real screen recording, no cuts.** Boundary date set to **2015-06-29**. The "HMM says" tile reads **TRANSITION**. Click *Run the pipeline*. The status log types out live: items retrieved, pages OK, `0` items post-dated the boundary and were dropped, date scrub → residual date signals, "Asking `claude-opus-5`…". Output appears: **Confidence: 🟢 high**, the Greek bailout summary, then the claims list with green ticks and the `cites …` id tags. Presenter expands one day of the window; cited items are highlighted red. | This is the live view, running now. I give it a date. It fetches the window, it checks the boundary, it strips absolute dates out of the text, and it sends it. The model gets news items labelled Day one to Day fourteen and nothing else. No prices. No ticker. No date. Here is what it returns. Eurozone finance ministers refusing to extend negotiations past a deadline. A referendum called on the creditors' terms. Deposit flight from Greek banks. Every sentence carries the id of the item it came from, and I can open that item and read it. The model described the news. The HMM decided the day. Those two jobs never touch. |
| **3:50–4:10**<br>(20s) | **The ordinary Tuesday.** Same Live view, no reload. Presenter clears the date and types **2019-12-03** — a Tuesday, a control date, no regime change. The "HMM says" tile flips to **no transition**. Click Run. Output appears: **Confidence: 🟡 medium**, driver identified, a fluent US–China escalation story — Huawei and ZTE designated national security risks, tariff threats — fully cited, every claim ticked green. Hold on that screen for the last three seconds in silence. | Now watch this. Third of December, twenty-nineteen. An ordinary Tuesday. The HMM says nothing happened. Same prompt, same pipeline, same permission to decline. And the model finds a story anyway. Medium confidence. Well sourced. Every claim grounded. It is not wrong — that news is real. But it is not evidence of anything, because there is always something to point at. |
| **4:10–4:50**<br>(40s) | Controls view → Blind matching tab. Table: all sections **11/20 = 55%** vs 5% chance, MRR 0.689, p < 0.0001; business only 4/14 = 28.6% vs 7.1%, p = 0.016; all sixty windows 33/60 vs 1.7%. Then the ceiling length-sweep bar chart. Cut to the Memorisation tab: **474 claims, 96.4% grounded, 0.0% fabricated citations**, plus the threshold-sensitivity table. | So how do I tell a real explanation from generic macro commentary? Two measurements. The first is blind matching. Take each explanation, strip the dates, scramble the order, and try to match it back to its own window — using held-out articles the model never saw, and BM25, which has no world knowledge and cannot date a paragraph from training the way an LLM judge could. Eleven out of twenty against one-in-twenty chance. Fifty-five percent. Permutation p below one in ten thousand. Pooled over all sixty windows, thirty-three out of sixty against one-in-sixty. So the explanations are specific. They contain real window-specific content. The second measurement is faithfulness. Four hundred and seventy-four individual claims, each checked lexically against the item it cited. Ninety-six point four percent grounded. Zero fabricated citations. Not one. |
| **4:50–5:40**<br>(50s) | Controls view → Placebo tab, full screen, no other windows. The arm table stays up for the whole segment: Real transitions **20 / 16 confident / 80.0%**; All controls **40 / 26 / 65.0%**; Clean controls **13 / 8 / 61.5%**. Then the two Fisher tiles: **p = 0.37**, **p = 0.43**. At 5:30 a single overlay line appears: *"Specific, accurate, well-sourced — and not diagnostic."* | And here is the result that matters most, which is the one that goes against me. Twenty real transitions, twenty-four era-matched control dates and their partners — sixty windows in total. Controls drawn from each transition's own neighbourhood, thirty to ninety days away, so the news environment and the era are matched. Year-mean gap of zero. Identical prompt. Identical pipeline. Only the news differs. On real transitions the model is confident eighty percent of the time. On control dates, sixty-five percent. On the clean stratum, sixty-one point five. Fisher exact test: p equals zero-point-three-seven, and zero-point-four-three on the pre-declared primary comparison. That is not significant. So this is the honest state of the method. The explanations are specific to their window — blind matching crushes chance. They are accurate and properly sourced — ninety-six percent grounded, zero fabricated citations. And they are not diagnostic of a regime change, because the model finds a confident story for an ordinary Tuesday almost as often as for a real one. You saw that happen. I did not want that result. It is the result. |
| **5:40–6:05**<br>(25s) | Memorisation tab, Label stability block. Three tiles — agreement on 'confident', agreement on driver identified, exact label agreement — with the measured numbers **[SLOT IN]**. Cut to `PROMPT_HISTORY.md`, the determinism note, then the Call log tab: rows of model id, prompt hash, input hash, token counts. | One more limit, because the placebo number is an LLM self-report. Sampling parameters have been removed from current Claude models, so there is no determinism knob and identical inputs no longer guarantee identical outputs. I ran all sixty windows a second time to find out how much of that confidence label is stable signal. Agreement on confident: **[X] percent**. Exact label: **[Y] percent**. So reproducibility here comes from the archive, not from re-execution. Every call is logged with the model id, the prompt hash, the input hash and the raw response. You can verify exactly what was sent. You cannot regenerate it byte for byte, and the report says so. |
| **6:05–6:35**<br>(30s) | **REQUIREMENTS (a) and (c).** Clean full-screen card, two columns, held for the whole segment.<br>**Built with:** Claude Code (Anthropic CLI) — code, tests, refactors; prompts hand-written and version-controlled in `prompts/`.<br>**Models, APIs, platforms:** `claude-opus-5` via the Anthropic Messages API, `effort: high`, schema-enforced structured output · Wikipedia MediaWiki API, revision-pinned · yfinance · hmmlearn · scikit-learn · Streamlit. Cursor highlights the one line in `settings.yaml`: `model: "claude-opus-5"`. | The build. The code, the test suite and the refactors were written with Claude Code, Anthropic's command-line agent, sitting in this repository. The prompts were not — they are hand-written, version controlled, and the reasoning behind every rule is in the prompt history. The narrative layer is claude-opus-five, called through the Anthropic Messages API, one call per window, no batching, reasoning effort high, with the output schema enforced server-side rather than asking the prompt nicely for valid JSON. News comes from the Wikipedia MediaWiki API, pinned by revision id. Prices from yfinance. The HMM is hmmlearn, scaling and metrics from scikit-learn, and this dashboard is Streamlit. The model name is one line in one settings file and appears nowhere else in the source. |
| **6:35–7:00**<br>(25s) | Back to the full regime chart from the cold open, now with the twenty transition lines. Three lines of text fade in one at a time over it: *The HMM decides **when**.* / *The language model describes **what was in the news**.* / *No number in the statistics comes from the language model.* Final frame: repo name and `streamlit run app.py`. | The architecture is one sentence. The statistical model decides when the regime changed. The language model only describes what was in the news at that moment. No number in the statistical results comes from the language model, and it never moves a regime boundary. What I have shown is a signal that survives being tested properly, an explanation layer that is specific and grounded, and a control that says that layer is not yet diagnostic. That last part is the finding. A method whose limit has been measured is worth more than one that has only been claimed. |

---

## Production notes (not part of the spoken runtime)

**Timing check:** 25+35+35+25+40+40+30+20+40+50+25+30+25 = **420s = 7:00**.

**The two Live-view takes (3:20 and 3:50) are the highest-risk shots.** Both call the API on camera. Mitigations:
- Calls are cached by input hash (`data/cache/llm_calls/`), so **run both dates once before filming**. The on-camera run then returns the identical cached response, fast and reproducible — and it is still the real pipeline, not a mock.
- Set the API key in the sidebar *before* recording starts so no key material is ever on screen.
- `2015-06-29` (Greece, `high`) is the strongest transition demo: the summary is concrete, dated-feeling without any date, and the claims are unusually crisp.
- `2019-12-03` for the ordinary-Tuesday shot: it is a genuine Tuesday, a control date, `medium` confidence, driver identified, and the story (Huawei/ZTE designation, US–China escalation) is fluent and fully cited — which is exactly the point. **Fallbacks if that window renders poorly on camera:** `2019-08-20` (Tuesday, `medium`, Italian government collapse) or `2016-05-03` (Tuesday, `medium`, Puerto Rico default). Avoid `2020-03-23` and `2016-06-28` — those controls landed on genuinely enormous days and undercut the "ordinary" framing.

**Slot-in at 5:40.** Replace `[X]` and `[Y]` with `agreement_is_confident` and `agreement_exact_confidence_label` from `outputs/retest_stability.json`. If the retest has not finished by the edit deadline, the segment still works with this substitute narration, same length: *"One more limit, because the placebo number is an LLM self-report. Sampling parameters have been removed from current Claude models, so identical inputs no longer guarantee identical outputs. A full test-retest of all sixty windows is running to measure how much of that confidence label is stable. Until it lands I will not quote a number for it. Reproducibility here comes from the archive, not from re-execution — every call is logged with the model id, the prompt hash, the input hash and the raw response."*

**Requirement coverage, for the judges' checklist:**
- **(a) AI coding tools** — 6:05–6:35, named on screen and spoken: Claude Code (Anthropic CLI), with the explicit boundary that prompts were hand-written.
- **(b) System prompts with examples** — 2:40–3:20 shows `prompts/narrative_v1.md` on screen with its sha256 and three rules called out with the failure mode each prevents; 3:20–3:50 and 3:50–4:10 show two real outputs of that exact prompt, including the cited items.
- **(c) Models, APIs, platforms** — 6:05–6:35 card: claude-opus-5 / Anthropic Messages API (effort high, schema-enforced structured output), Wikipedia MediaWiki API (revision-pinned), yfinance, hmmlearn, scikit-learn, Streamlit.

**Files referenced on camera (all absolute):**
- `C:/Users/sze ming chak/Documents/GitHub/regime-narrative/prompts/narrative_v1.md`
- `C:/Users/sze ming chak/Documents/GitHub/regime-narrative/docs/PROMPT_HISTORY.md`
- `C:/Users/sze ming chak/Documents/GitHub/regime-narrative/settings.yaml`
- `C:/Users/sze ming chak/Documents/GitHub/regime-narrative/src/regime_narrative/hmm_model.py`
- `C:/Users/sze ming chak/Documents/GitHub/regime-narrative/src/regime_narrative/news/base.py`
- `C:/Users/sze ming chak/Documents/GitHub/regime-narrative/app.py` (Overview, Controls, Live, Provenance views)